import styles from './page.module.css';

const Admin = async () => {
  return (
    <main className={styles.Admin}>
      <div>
        Admin page
      </div>
    </main>
  );
}

export default Admin;